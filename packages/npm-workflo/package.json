{
  "name": "@cortex/workflo",
  "version": "0.2.0",
  "description": "workflo — sandboxed code-testing agent with signed receipts. npm distribution shim; the engine ships as prebuilt Python wheels in a package-local venv.",
  "license": "Proprietary",
  "bin": {
    "workflo": "bin/workflo.js"
  },
  "files": [
    "bin",
    "scripts",
    "vendor"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "pack:build": "python scripts/build-wheels.py && npm pack --pack-destination dist",
    "test": "node scripts/smoke-test.js"
  }
}
