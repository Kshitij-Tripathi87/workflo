"""Auto-generated conftest for the sample_repo fixture.

Path setup is handled by `[tool.pytest.ini_options] pythonpath = ["src"]` in
`pyproject.toml` (pytest>=7 native feature, more reliable than sys.path
mutation here). This file exists only to be a stable hook for future
fixture-level fixtures; it intentionally does nothing right now so the
3-test baseline stays deterministic.
"""
